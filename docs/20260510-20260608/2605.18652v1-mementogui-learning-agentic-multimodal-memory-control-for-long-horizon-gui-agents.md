---
title: "MementoGUI: Learning Agentic Multimodal Memory Control for Long-Horizon GUI Agents"
title_zh: MementoGUI：面向长时域GUI智能体的智能多模态记忆控制学习
authors: "Ziyun Zeng, Hang Hua, Bocheng Zou, Mu Cai, Rogerio Feris, Jiebo Luo"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18652v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 面向GUI智能体的学习型记忆控制器，实现选择、压缩和检索
tldr: 现有GUI代理在长时任务中因依赖原始历史或纯文本记忆而表现脆弱。MementoGUI通过MementoCore学习控制器实现在线记忆选择、压缩与检索，将长时控制形式化为工作记忆与情景记忆的协同管理。在GUI-Odyssey、MM-Mind2Web和MementoGUI-Bench上，MementoGUI持续超越无历史、历史回放和纯文本记忆基线，且更大骨干可进一步提升性能。该方法即插即用，无需微调主干，并提供了评测基准，为长时GUI代理的记忆增强提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18652v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1318, \"height\": 854, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18652v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1407, \"height\": 823, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18652v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1432, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18652v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1443, \"height\": 514, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.18652v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 916, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18652v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1445, \"height\": 215, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18652v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 719, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18652v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 722, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18652v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1453, \"height\": 436, \"label\": \"Table\"}]"
motivation: 现有GUI代理在处理长时跨界面任务时，因缺乏有效记忆管理而表现脆弱，无法维持任务状态。
method: MementoGUI通过MementoCore控制在线记忆选择、压缩与检索，结合工作记忆和情景记忆增强GUI代理，无需微调主干。
result: 在GUI-Odyssey、MM-Mind2Web和MementoGUI-Bench上，MementoGUI优于无历史、历史回放和纯文本记忆基线。
conclusion: MementoGUI通过即插即用记忆增强有效提升长时GUI任务性能，且更大骨干可进一步改善结果。
---

## 摘要
最近的GUI智能体在视觉定位和动作预测方面取得了显著进展，但在需要跨多个界面转换维持任务状态的长时域任务中仍然脆弱。现有的智能体通常依赖原始历史回放或纯文本记忆，这要么用冗余截图淹没模型，要么丢弃未来决策所需的局部视觉证据。为解决这些限制，我们提出了MementoGUI，一个即插即用的智能体记忆框架，它为基于MLLM的GUI智能体配备了MementoCore，一个用于在线记忆选择、压缩和检索的学习控制器。MementoGUI不是将交互历史视为固定上下文，而是将长时域GUI控制形式化为一个在线记忆控制问题：工作记忆通过文本摘要和ROI级视觉证据选择性保留任务相关的界面事件，而情景记忆通过学习的相关性选择检索可重用的过去轨迹。MementoCore将记忆控制模块化为专门的操作符，用于步骤处理、记忆压缩、情景写入和情景选择，从而实现即插即用的记忆增强，而无需微调GUI智能体主干的参数。我们进一步开发了一个可扩展的数据整理流程，将计算机使用轨迹转换为记忆控制器训练数据，引入了MementoGUI-Bench用于评估GUI智能体的长时域决策能力，并设计了基于MLLM的指标用于语义动作匹配、任务进度和记忆一致性。在GUI-Odyssey、MM-Mind2Web和MementoGUI-Bench上的实验表明，MementoGUI在无历史、历史回放和纯文本记忆基线上始终改善了GUI智能体的性能，更大的MementoCore主干进一步强化了记忆增强的GUI控制。

## Abstract
Recent GUI agents have made substantial progress in visual grounding and action prediction, yet they remain brittle in long-horizon tasks that require maintaining task state across many interface transitions. Existing agents typically rely on raw history replay or text-only memory, which either overwhelms the model with redundant screenshots or discards localized visual evidence needed for future decisions. To address these limitations, we introduce \textbf{MementoGUI}, a plug-in agentic memory framework that equips MLLM-based GUI agents with \textbf{MementoCore}, a learned controller for online memory selection, compression, and retrieval. Rather than treating interaction history as a fixed context, MementoGUI formulates long-horizon GUI control as an online memory-control problem: working memory selectively preserves task-relevant interface events with textual summaries and ROI-level visual evidence, while episodic memory retrieves reusable past trajectories through learned relevance selection. MementoCore modularizes memory control into specialized operators for step processing, memory compression, episodic writing, and episodic selection, enabling plug-in memory augmentation without finetuning the GUI agent backbone. We further develop a scalable data curation pipeline that converts computer-use trajectories into memory-controller training data, introduce \textbf{MementoGUI-Bench} for evaluating long-horizon decision-making in GUI agents, and design MLLM-based metrics for semantic action matching, task progress, and memory consistency. Experiments on GUI-Odyssey, MM-Mind2Web, and MementoGUI-Bench show that MementoGUI consistently improves GUI agents over no-history, history-replay, and text-only memory baselines, with larger MementoCore backbones further strengthening memory-augmented GUI control.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于多模态大语言模型（MLLM）的GUI智能体在单步视觉定位和动作预测上已有进步，但在需要跨大量界面转换维持任务状态的长时域任务中仍表现脆弱。智能体依赖原始历史回放（冗余截图）或纯文本记忆（丢失局部视觉证据），导致任务约束遗忘、进度丢失、动作重复等失败模式。
- **整体含义**：作者指出长时域GUI控制的瓶颈已从单步理解转向跨步状态管理，应被视为一个**在线多模态记忆控制问题**，而非简单的上下文长度扩展。论文提出通过学习的记忆控制器主动管理交互历史，而非被动依赖更长上下文。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：MementoGUI是一个**即插即用的记忆增强框架**，用于冻结的GUI动作主干。它包含一个学习控制器MementoCore，维护两种互补记忆：**工作记忆**（追踪当前任务内的演变状态）和**情景记忆**（存储跨任务的可重用经验）。控制器在每一帧决定如何更新、压缩、写入和检索记忆，并将结构化多模态上下文（文本摘要+ROI截图）注入主干提示。
- **关键技术细节**：
  - **MementoCore**：基于冻结的Qwen3-VL骨干，通过四个独立LoRA适配器实现四个算子：
    - *Step Processor*：对当前截图和上一动作输出重要性分数$o_t$、事件摘要$s_t$、ROI框$b_t$、是否触发情景检索$\gamma_t$。
    - *WM Compressor*：当工作记忆超出容量时，压缩较旧条目为紧凑摘要并保留代表性ROI标识。
    - *Episodic Writer*：在轨迹结束后将完整轨迹压缩为情景记忆条目（摘要、元数据、ROI）。
    - *Episodic Selector*：对粗检索到的候选情景条目进行多模态相关性过滤。
  - **记忆操作流程**（每步）：
    1. Step Processor输出信息，若重要性$o_t > \tau$则将条目$e_t=(s_t,b_t,r_t)$追加到工作记忆。
    2. 若$\gamma_t=1$或第一步，则从情景记忆中检索：先通过视觉+文本双模态嵌入进行粗检索，再通过Selector细过滤。
    3. 输入构造器将当前截图$x_t$、ROI图像集$V^{\text{mem}}_t$和文本上下文$c_t$（包含目标、工作记忆文本摘要、情景检索文本摘要）组装为标准多模态对话模板，送入冻结的GUI主干得到动作$a_t$。
  - **训练**：通过自动数据整理流程从PSAI计算机使用轨迹生成SFT数据（用于四个算子），并额外对Step Processor和WM Compressor构建DPO偏好数据（通过规则破坏和VLM评判）。训练使用LoRA，不更新Qwen3-VL主干参数。

### 3. 实验设计

- **使用的数据集**：
  - **GUI-Odyssey**：跨应用移动GUI导航数据集。
  - **MM-Mind2Web**：多模态web任务数据集。
  - **MementoGUI-Bench**：作者新构建的长时域记忆依赖基准，从PSAI数据抽取200条轨迹（80测试+120用于情景记忆积累），平均每轨迹34.8步。
- **对比方法**：
  - 冻结开源GUI主干：UI-Venus-1.5-8B、MAI-UI-8B、GUI-Owl-1.5-8B、GUI-Owl-1.5-32B。
  - 记忆增强策略：No History、Pred. Hist. All（预测全部历史）、Text Summary Memory（纯文本摘要）、Working Memory（仅工作记忆）、Working + Episodic Memory（完整MementoGUI）。
  - 闭源基线：GPT-5.5和Gemini-3.1-Pro直接提示。
- **评估指标**：
  - 标准指标：动作匹配分数(AMS)、步骤成功率(Step SR)、轨迹成功率(Traj. SR)。
  - 新增MLLM指标（Gemini-3.1-Pro评判）：语义动作匹配(VAM)、任务进度分数(TPS)、记忆一致性分数(MCS)。

### 4. 资源与算力

- 文中**未明确说明**训练MementoCore所使用的GPU数量、型号及训练时长。仅在推理延迟测量中提到使用NVIDIA H100 GPU（表5给出端到端轨迹推理时间，如UI-Venus-1.5-8B+2B控制器+工作记忆：41.98s/轨迹）。训练使用PSAI数据集的轨迹量（未具体列出数量），但数据预处理和SFT/DPO训练成本未给出。可以推测训练规模相对较小（LoRA微调）。

### 5. 实验数量与充分性

- **实验组数**：
  - 主实验（表1）：4个开源骨干 × 5种方法 × 3个基准 = 60个条件对比（部分骨干只测部分方法）。
  - API代理增强实验（表2）：2个闭源模型 × 2种方法（直接提示 vs 工作记忆） × 2个基准。
  - 轨迹长度分析（图3）：GUI-Odyssey上按长度分桶（1-59步）展示AMS。
  - 情景记忆库大小（图4）：不同库大小对GUI-Odyssey的影响。
  - 视觉记忆消融（表3）：UI-Venus-1.5-8B和GUI-Owl-1.5-8B上，对比Text Summary Memory、WM w/o Visual Memory、Full Working Memory。
  - 检索策略消融（表4）：同样两个骨干，对比WM Only、随机情景、单阶段检索、两阶段检索。
  - 控制器规模缩放（表5）：2B/4B/8B × 两种骨干 × 两种记忆配置（WM vs WM+EM）在GUI-Odyssey和MementoGUI-Bench上，报告时间和性能。
- **充分性评价**：实验全面覆盖不同主干、不同记忆策略、不同基准，并且做了充分的消融（视觉、检索、规模）。指标多样包括标准和新设计的。公平性方面，所有GUI主干保持冻结，记忆控制器即插即用，对比基线设置合理。新增基准MementoGUI-Bench专门针对长期记忆依赖。因此**实验设计充分且客观公平**。

### 6. 论文的主要结论与发现

1. MementoGUI在所有冻结GUI主干上**一致优于**无历史、历史回放和纯文本记忆基线。例如，UI-Venus-1.5-8B在GUI-Odyssey上AMS从54.58提升至68.32（工作+情景记忆），轨迹成功率从1.29提升至3.57。
2. 工作记忆与情景记忆的组合提供**互补增益**。单独工作记忆已显著提升，加入情景记忆进一步改善。
3. **视觉ROI证据**不可或缺：去除ROI图像后性能下降（表3）。
4. **两阶段检索**（粗检索+细过滤）优于单阶段检索或随机注入（表4）。
5. **更大的记忆控制器**（8B vs 2B）带来更强的长时决策支持，且无需微调动作主干（表5）。
6. 记忆增强对**闭源API代理**也有积极作用（表2），尤其是在任务进度和记忆一致性方面。

### 7. 优点

- **即插即用**：无需修改或微调GUI动作主干，降低采用成本。
- **模块化设计**：四个算子分工明确，可独立训练，易于扩展。
- **自动数据整理**：从原始视频轨迹自动生成结构化监督信号，减少人工标注。
- **新基准与细粒度指标**：MementoGUI-Bench和MLLM评判的VAM/TPS/MCS为长期记忆评估提供了更精准的测量。
- **实验充分且消融严谨**：涵盖多种骨干、策略、规模，清晰展示了各组件贡献。
- **通用性**：在移动和Web环境均有效，且可增强开源和闭源模型。

### 8. 不足与局限

- **算力开销未明确**：训练资源的缺乏详细说明，不利于复现评估。
- **计算延迟增加**：表5显示加入记忆控制器后轨迹推理时间显著上升（例如从40s增至50-70s），在实时场景可能成为瓶颈。
- **依赖离线轨迹训练**：数据来自PSAI的固定轨迹，未探索在线交互中记忆控制器的自适应学习；可能存在分布外问题。
- **情景记忆的搜索规模**：当前实验仅使用了120条情景库，更大规模（如数千条）下的检索效率和选择性过滤效果未测试。
- **评估指标依赖VLM判断**：VAM/TPS/MCS由Gemini-3.1-Pro评判，可能存在评判偏差，且未与其他评判方式（人类标注）对比校准。
- **未涉及多模态内容之外的维度**：如无障碍、多语言、跨平台等特定场景未测试。
- **只适用于有清晰动作轨迹的场景**：对开放式探索或多条有效路径的任务，严格参考匹配（如AMS）可能不公平，虽然VAM部分缓解。

---

（完）
