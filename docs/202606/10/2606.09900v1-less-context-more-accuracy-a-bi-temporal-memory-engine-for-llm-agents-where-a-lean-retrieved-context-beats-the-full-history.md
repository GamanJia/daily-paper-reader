---
title: "Less Context, More Accuracy: A Bi-Temporal Memory Engine for LLM Agents Where a Lean Retrieved Context Beats the Full History"
title_zh: 更少上下文，更高精度：面向LLM Agent的双时序记忆引擎，轻量检索上下文胜过完整历史
authors: Liuyin Wang
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.09900v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 面向LLM代理的双时间记忆引擎，支持长期记忆
tldr: "LLM智能体长时记忆缺失，回放全部历史成本高且准确度低。Engram提出双时间记忆引擎，通过异步构建知识图与混合检索，在约9.6k token切片上达到83.6%准确率，超越全历史基线（73.2%），且token减少8倍。该方法证明了精简上下文优于完整历史的可能性，并提供了可复现的评估工具。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09900v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1687, \"height\": 911, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09900v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1641, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09900v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1163, \"height\": 779, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09900v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1364, \"height\": 745, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09900v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 733, \"height\": 149, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09900v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1472, \"height\": 210, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09900v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1678, \"height\": 1156, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09900v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1295, \"height\": 577, \"label\": \"Table\"}]"
motivation: 现有记忆系统在成本或延迟上优化，但准确率不及全历史基线，且评估不一致。
method: 双进程引擎：快速写入无LLM，异步提取原子事实构建双时间知识图；混合读取融合多信号并作时间过滤。
result: "在LongMemEval_S上，Engram以9.6k token检索切片达83.6%，超越全历史73.2%（+10.4），token减少8倍，零错误。"
conclusion: 开源Engram及中立评估工具，证实精简上下文可提升准确率与效率，并揭示基准测试中的常见偏误。
---

## 摘要
长期记忆是LLM Agent缺失的一层：跨会话时它们会遗忘，常见的解决方法——将整个历史重放到提示词中——代价高昂、速度缓慢，并且随着干扰项积累，精度下降。大多数记忆系统在成本或延迟上胜出，但在精度上仍输给全上下文基线，而且基准测试结果在不一致、不可复现的测试框架上报告，导致同一系统在不同来源上得分差异巨大。我们提出了Engram，一个基于双时序数据模型的开源双过程记忆引擎。快速写入路径以无损方式追加事件，关键路径上不调用LLM；异步路径提取原子事实（主语、谓语、宾语），构建双时序知识图谱，并解决矛盾而不对每个事实调用LLM——通过作废而非删除，因此每个事实都保留来源和取代链。混合读取路径融合稠密、词汇、图以及新鲜度/显著性信号，应用时间点（“as-of”）过滤，并组装紧凑且带有来源标签的上下文。在完整的500个问题的LongMemEval_S数据集上，由官方类别特定评判器评分，Engram的轻量配置——从约9.6k token的检索片段中作答，从不使用完整历史——得分83.6%，而全上下文为73.2%（+10.4个百分点，McNemar p < 10^-6），token数减少约8倍（9.6k vs 79k），且0/500错误。这一提升需要混合读取路径：仅靠事实会丢失召回率，而事实加检索片段可恢复细节。我们还贡献了一个中立的、仓库内的评估框架，内置官方评判器并在每个表格中包含全上下文基线，发布每道题的原始日志，并记录了静默扭曲记忆基准的测量完整性陷阱（截断、自建评判器、完整历史泄露）。每个数字都附带可复现的命令。

## Abstract
Long-term memory is the missing layer for LLM agents: across sessions they forget, and the common workaround -- replaying the whole history into the prompt -- is expensive, slow, and, as distractors accumulate, less accurate. Most memory systems win on cost or latency but still lose to the full-context baseline on accuracy, and benchmark numbers are reported on inconsistent, non-reproducible harnesses, so one system appears at wildly different scores across sources. We present Engram, an open-source, dual-process memory engine on a bi-temporal data model. A fast write path appends lossless episodes with no LLM on the critical path; an asynchronous path extracts atomic (subject, predicate, object) facts, builds a bi-temporal knowledge graph, and resolves contradictions without an LLM call per fact -- invalidating, never deleting, so every fact keeps provenance and a supersession chain. A hybrid read path fuses dense, lexical, graph, and recency/salience signals, applies a point-in-time ("as-of") filter, and assembles a compact, provenance-tagged context. On the full 500-question LongMemEval_S, graded by the official category-specific judge, Engram's lean configuration -- answering from a ~9.6k-token retrieved slice, never the full history -- scores 83.6% vs. 73.2% for full-context (+10.4 points, McNemar p < 10^-6) at ~8x fewer tokens (9.6k vs. 79k), with 0/500 errored. The gain needs a hybrid read path: facts alone lose recall, while facts plus retrieved chunks recover detail. We also contribute a neutral, in-repo evaluation harness with the official judge baked in and the full-context baseline in every table, publish the raw per-question logs, and document the measurement-integrity pitfalls (truncation, home-grown judges, full-history leaks) that silently distort memory benchmarks. Every number ships with a command to reproduce it.

---

## 论文详细总结（自动生成）

好的，以下是基于您提供的论文内容生成的详细中文总结。

### 论文总结：Less Context, More Accuracy: A Bi-Temporal Memory Engine for LLM Agents

#### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型（LLM）Agent 缺乏真正的长期记忆。它们在跨会话交互后会遗忘之前的信息。
- **现有方法的缺陷**：
    - **“回放全部历史”（Full-Context）方法**：将整个对话历史直接拼接到提示词（Prompt）中。这种方法存在三大问题：a) **成本高**：Token消耗随历史增长线性增长；b) **速度慢**：延迟随之增加；c) **精度低**：大量无关的干扰项（Distractors）会淹没关键信息，使模型表现变差（即“迷失在中间”问题）。
    - **现有记忆系统**：虽然通常在成本或延迟上优于全历史方法，但**在准确率上仍无法超越**全历史基线。此外，该领域的基准测试（Benchmark）评估标准不统一、不可复现，导致同一系统在不同报告中的得分差异巨大，难以客观比较。
- **论文的整体意义**：
    - **核心论点**：一个**精确检索的、精简的上下文切片（Lean Retrieved Context）可以比嘈杂的完整历史更准确**。这不仅是成本优化，更是**质量提升**。
    - **贡献**：提出了一个新颖的双时序（Bi-Temporal）记忆引擎 **Engram**，并配套提供了一个**中立、可复现的评估工具**，旨在解决上述准确性和可复现性问题。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：采用 **双过程（Dual-Process）架构**，区分快速写入和异步知识提取。
- **关键技术细节**：
    1.  **双时序数据模型**：
        - 所有数据都带有两个时间轴：**事件/有效时间**（世界真实发生的时间）和**记录/事务时间**（系统记录的时间）。
        - **Episode（事件）**：原始的无损对话轮次，带有两个时间戳。
        - **Fact（事实）**：原子化的`（主语，谓语，宾语）`三元组，同样带有双时间戳，并包含一个**取代指针（Supersedes Pointer）**。
        - **核心规则**：事实不会被物理删除，当新事实与旧事实冲突时，旧事实被标记为**无效（Invalidated）**，并指向新事实。这保证了所有历史的完整可溯源性。
    2.  **System-1：快速写入路径**：
        - 在`add(messages)`时，**无LLM调用**。
        - 操作包括：追加无损Episode、身份识别、生成轻量级嵌入、将Episode放入异步队列。耗时 < 50ms。
    3.  **System-2：异步整合路径**：
        - 在离线后台运行，从Episode中：a) 提取原子事实（基于规则或LLM）；b) 构建双时序知识图谱；c) **检测并解决事实冲突**；d) 计算并衰减显著性（Salience）。
    4.  **“低成本-分级”冲突解决机制**：
        - 输入新事实时，按成本递增顺序处理：a) 精确匹配（更新）；b) 清晰矛盾且时间有序（直接作废旧事实，无需LLM）；c) 只有真正模棱两可的情况才调用LLM仲裁。这是一种效率优先的设计。
    5.  **混合读取路径（Hybrid Read Path，核心贡献之一）**：
        - `search(query)` 时，系统进行多通道并行检索并融合：a) **稠密语义检索**；b) **词法BM25检索**；c) **图关系检索**；d) **时间/显著性检索**。
        - 使用 **倒数排序融合（RRF）** 和可选的交叉编码器重排序来融合结果。
        - 应用**双时序“as-of”过滤器**：只返回在指定时间点有效的事实。
        - 通过**弃权门（Abstention Gate）**：在证据不足时拒绝回答，避免幻觉。
        - **组装混合上下文**：最终提供给答案模型的上下文不仅包含冲突解决后的原子**事实**（结构化），还包含最相关的原始对话**片段**（Chunks）和**会话摘要**（非结构化）。**论文强调，“事实+片段”的混合结构是准确率提升的关键。**

#### 3. 实验设计：数据集、基准与对比方法
- **数据集**：使用 **LongMemEval S** 基准测试，包含 **500个问题**，每个问题基于约50个会话（约115k tokens）的历史。问题覆盖7个类别：单会话、多会话、知识更新、时间推理、偏好、弃权等。
- **评估基准（Judge）**：
    - **官方评判器**：使用LongMemEval官方提供的、**类别特定的评判提示词**，而不是自制的通用评判器，保证了评估的公平性和可比性。
- **对比方法**：
    - **Engram Lean（标题系统）**：从混合检索切片（约9.6k tokens）中作答，从不查看完整历史。
    - **Full-Context Baseline（全历史基线）**：将整个历史（约79k tokens）放入提示词中。这是公平比较的关键，确保在相同条件下进行。
    - **Engram Full（消融变体）**：将事实提炼结果放在完整历史之上（约79k tokens），用于验证事实本身的增益。
- **模型配置**：
    - **Embedder**：BAAI/bge-small-en-v1.5。
    - **事实提取器**：doubao-seed-1.6-flash。
    - **答案生成器**：doubao-seed-2.0-pro。
    - **评判器**：DeepSeek-V3.2。
- **公平性保障**：
    - 同一个答案生成器和评判器应用于所有对比系统。
    - 测量了并修复了多个常见的“测量完整性陷阱”，例如：基线不是真正的“全历史”（如被截断）、使用自制评判器、泄露完整历史等。

#### 4. 资源与算力
- **未明确说明**：论文中**没有提及**训练Engram模型所需的GPU型号、数量和训练时长。论文的重点是系统设计和评估，而非模型预训练。Engram本身是一个框架，其核心模型（如Embedder和LLM）是现成的。
- **可复现性**：论文强调，Engram可以**零设置运行**，无需API密钥，通过确定性的离线回退机制（如哈希嵌入器、基于规则的事实提取器）即可开箱测试整个架构。

#### 5. 实验数量与充分性
- **主要实验**：在**完整的500个问题**上进行了主要对比实验，结果是统计显著的。
- **消融实验分析**：通过`engram_full`变体表明，**结构化事实是准确率提升的主要来源**。通过开发过程中的观察指出，**混合读取路径（事实+原始片段）是必要的**，仅依赖事实会丢失细节。
- **类别分析**：对7个类别进行了细粒度分析，揭示了系统在知识更新和时间推理任务上的优势，以及在多会话聚合和偏好任务上的不足。
- **充分性评估**：
    - **优点**：实验设计客观、公平，使用了官方评判器和统一的基线。实验结果统计显著，并公布了完整的原始日志以供复现。
    - **不足/局限**：**实验覆盖范围有限**。论文承认当前结论仅基于**一个基准（LongMemEval S）** 和**一个答案生成器模型**。**没有进行受控的完整消融实验**，即没有在完整的500个问题上，系统性地移除读取路径的各个组件（如仅事实、仅片段）来量化它们各自的贡献。**缺乏多轮运行（Run-to-run Variance）的报告**，以衡量答案生成器随机性带来的波动。

#### 6. 主要结论与发现
1.  **精简上下文超越完整历史**：Engram的`engram_lean`配置在LongMemEval S上的准确率为 **83.6%**，显著高于全历史基线的 **73.2%**（+10.4个百分点），同时使用了**约8倍更少的Token**（9.6k vs 79k）。这证明了去除干扰、精准检索的切片可以比嘈杂的完整窗口更准确。
2.  **混合读取路径是刚需**：准确率的提升依赖于“事实+原始片段”的混合读取路径。仅依赖原子化事实会丢失细节，导致召回率下降。
3.  **双时序模型在特定类别上效果显著**：在需要追踪知识更新（87.5%）和时间推理（81.1%）的任务上，Engram的表现尤为突出，这得益于其“as-of”查询能力。
4.  **弃权门有效**：在无答案问题上，系统能有效识别并拒绝回答（准确率86.7%），减少了幻觉。
5.  **可复现性声明**：论文通过提供开源工具、官方评判器、完整日志和可复现命令，为该领域的公平比较树立了榜样。

#### 7. 优点
- **方法论新颖**：双时序数据模型和低成本冲突解决机制是工程和设计的亮点，使其既高效又准确。
- **实验结果有力**：证明了精简上下文可以超越完整历史这一反直觉但至关重要的发现，将记忆系统从成本优化工具提升为质量提升工具。
- **实验设计严谨**：对基准测试中的常见陷阱（如不完整基线、自制评判器）进行了识别和修正，确保了对比的客观性和公平性。提供完全可复现的评估工具是该论文的一大贡献。
- **实践导向**：系统是开源的，可以零设置运行，提供了清晰的架构和接口，易于集成和扩展。

#### 8. 不足与局限
- **实验覆盖不广**：结论仅基于**一个基准（LongMemEval S）** 和**一个答案生成器模型**（doubao-seed-2.0-pro）。其泛化能力有待在更多基准（如LOCOMO）和不同模型（如小模型、前沿模型）上验证。
- **缺乏完整的组件消融**：论文中的“事实+片段”混合路径的关键性结论是基于开发过程中的观察，而不是一个在完整500问题集上进行的受控、定量消融实验。这降低了结论的统计确定性。
- **缺乏多轮运行分析**：没有量化同一系统多次运行结果的方差。由于LLM输出具有随机性，单次运行的结果可能不可靠。
- **存在未攻克的高难度类别**：在多会话聚合和偏好回忆任务上，Engram仍有较大提升空间。
- **未提及训练资源**：没有讨论训练底层嵌入模型或大型语言模型的成本和算力需求。

（完）
