---
title: "ConMem: Structured Memory-Guided Adaptation in Training-Free Multi-Agent Systems"
title_zh: ConMem：无训练多智能体系统中的结构化记忆引导适应
authors: "Zhixun Tan, Qiang Chen, Tairan Huang, Xiu Su, Yi Chen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08702v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 提出利用结构化记忆卡和关系感知记忆图的无训练多代理适应框架
tldr: "针对LLM多智能体系统在自适应中面临的轨迹噪声、记忆-技能关系建模不足及依赖额外训练等问题，提出无需训练的ConMem框架。该方法将历史交互提炼为结构化记忆卡片，构建关系感知图实现跨经验协调。实验表明，ConMem在多个基准和架构上持续提升性能，推理时剪枝超50%候选，规划开销降低80%以上。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08702v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1427, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1449, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 705, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1438, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1038, \"height\": 2033, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1040, \"height\": 2032, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1041, \"height\": 2034, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1040, \"height\": 2031, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08702v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1466, \"height\": 1306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08702v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1555, \"height\": 246, \"label\": \"Table\"}]"
motivation: 现有记忆方法受限于噪声轨迹、记忆技能关系建模不足，且依赖额外训练或高质量监督。
method: ConMem通过结构化记忆卡片和关系感知图，在运行时检索并协调卡片以解决策略冲突与依赖。
result: "多个基准和主流架构上一致超越现有方法，推理时剪枝超50%候选，规划开销降低80%以上。"
conclusion: 无需训练的结构化记忆图使MAS实现鲁棒、轻量级自适应，显著提升效率。
---

## 摘要
近期进展通过基于记忆、技能和学习的方法提高了基于LLM的多智能体系统（MAS）的适应能力，但这些方法仍面临噪声轨迹、记忆-技能关系建模不足以及对额外训练或高质量监督的依赖等挑战。为解决这些限制，我们提出了ConMem，一种关系感知且无需训练的框架，通过跨经验协作实现高效的多智能体适应。具体来说，ConMem将历史交互轨迹提炼为结构化记忆卡片以捕获可重用策略和线索，并将其组织成关系感知的记忆图。在运行时，ConMem根据任务需求检索卡片，并通过卡片图协调它们以解决策略冲突并恢复其依赖关系。结合这些模块，提供了结构化和关系感知的指导，实现多智能体系统中稳健、轻量级的适应而无需额外训练。在多个基准和主流MAS架构上的大量实验表明，与现有记忆架构相比，ConMem取得了持续改进，通过剪枝超过50%的扩展候选并减少超过80%的规划开销，提高了推理效率。我们的代码可在https://anonymous.4open.science/r/ConMemCode获取。

## Abstract
Recent advances have improved the adaptive capabilities of LLM-based multi-agent systems (MAS) through memory-, skill-, and learning-based approaches, yet these approaches remain challenged by noisy trajectories, insufficient modeling of memory-skill relations, and reliance on additional training or high-quality supervision. To address these limitations, we propose ConMem, a relation-aware and training-free framework that enables efficient multi-agent adaptation through cross-experience coordination. Specifically, ConMem distills historical interaction trajectories into structured memory cards to capture reusable strategies and cues, organizing them into a relation-aware memory graph. At runtime, ConMem retrieves cards according to task needs and coordinates them through the card graph to resolve strategy conflicts and recover their dependencies. Combined, these modules yield structured and relation-aware guidance, enabling robust, lightweight adaptation in multi-agent systems without additional training. Extensive experiments across multiple benchmarks and mainstream MAS architectures show consistent gains over existing memory architectures, with improved inference-time efficiency through pruning more than 50% of expanded candidates and reducing planning overhead by over 80%. Our codes are available at https://anonymous.4open.science/r/ConMemCode

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：基于LLM的多智能体系统（MAS）在复杂持续任务中表现出色，但现有记忆驱动的自适应方法面临三大挑战：  
  - 历史轨迹长且包含噪声、局部工具结果、分歧和过时信息，导致决策信号扭曲、检索精度低。  
  - 技能/过程性记忆方法虽能减少冗余，但缺乏运行时的关系感知协调，未解决冲突、依赖和冗余问题。  
  - 训练驱动的方法（如强化学习、记忆进化）虽能灵活调整行为，但依赖额外训练或高质量监督，增加计算和实现复杂度。  

- **整体含义**：本文旨在回答“如何在没有额外训练的情况下，将碎片化的经验转化为结构化的、可协调的可复用集体记忆，实现高效的多智能体自适应”。提出**ConMem**框架，通过关系感知且无需训练的跨经验协调，实现轻量、高效的自适应。

### 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将历史交互轨迹抽象为**结构化记忆卡片**（typed and signed memory cards），编码状态（state）、计划（plan）、执行（execution）和评估（evaluation）信息；卡片组织成**关系感知的记忆图**；运行时根据任务需求检索卡片，通过图协调解决冲突、恢复依赖，并在预算约束下组合成前缀注入冻结的MAS主机。

- **关键技术细节**：  
  - **卡片（Card）**：原子可复用策略单元，包含任务描述、触发语义、四个字段（state/plan/exec/eval）及符号（正/负）。正卡片记录成功策略，负卡片记录失败警告。  
  - **记忆图（Memory Graph）**：边类型包括 supports（支持）、satisfies（满足）、constrains（约束）、conflicts（冲突），边有权重。  
  - **更新管线（Write Path）**：由反思（Reflection）、聚合（Aggregation）、准入（Admission）、合并（Merging）组成。使用质量评分函数Q(c) = λ₁C(c) + λ₂N(c) + λ₃R(c) + λ₄U(c) 筛选高质量卡片。  
  - **读取管线（Read Path）**：  
    1. **需求感知检索（Needs-aware Retrieval）**：根据任务软需求剖面πt，结合语义相似度、触发匹配、需求对齐、可信度、质量等加权评分排序，并应用MMR去重。  
    2. **类型图扩展（Typed-graph Expansion）**：从种子卡片沿正边（supports/satisfies）进行有界层式闭包扩展，避免冲突。  
    3. **协调与预算组合（Coordinated Budgeted Composition）**：去除冗余、解析局部冲突、延迟不活跃约束，在token预算内序列化卡片。  

- **公式与算法流程**：  
  - 全局目标：max Φ E_t[R(y_t | ˆx_t)] s.t. ℓ(˜m_t) ≤ B，其中˜m_t是组合后的记忆前缀。  
  - 读取管线：U_Φ = PB_mem ◦ KG_t ◦ EG_t ◦ RB_t（预算组合◦协调◦图扩展◦检索）。  
  - 更新管线：W_Φ = Mrg ◦ Adm ◦ Agg ◦ Ref。  
  - 负卡片激活阈值：p > (G+λ_ℓ)/(L+G)，其中p为有害后验概率，L为有害损失，G为误报损失，λ_ℓ为记忆成本。  
  - 定理1：若卡片提取ψ充分，则贝叶斯最优决策可仅依赖卡片，且预算可行域扩大。  
  - 定理2：需求估计误差导致检索遗憾上界为2γk∥w_t-π_t∥₁。

### 3. 实验设计：数据集/场景、基准、对比方法

- **数据集与场景**：  
  - 知识密集型QA：**TriviaQA** (6993任务) 和 **PopQA** (7267任务)  
  - 代码生成：**KodCode** (2000任务)  
  - 符号规划：**PDDL** via PDDLGym (60任务)  

- **基准（Benchmark）**：四类任务，侧重不同自适应挑战：知识检索、代码修复策略复用、规划状态记忆。  

- **对比方法**：  
  - 无记忆主机（No-memory）  
  - 框架基线：ChatDev, MetaGPT, JoyAgent, OAgents  
  - 训练无关记忆/技能基线：Generative Agents, Voyager, SimpleMem, G-Memory, ReMe  
  - 可学习记忆基线：LatentMem  

- **评估设置**：  
  - 所有方法共享相同主机（AutoGen为主，CAMEL、MacNet为未见主机）、相同LLM骨干（Qwen/Qwen3-4B-Instruct-2507）、相同提示预算、相同任务顺序、相同检索设置。  
  - 指标：QA用准确率、KodCode用单元测试通过率、PDDL用归一化规划得分。  
  - 评估方式：序列化评估（prequential），任务t仅使用之前提交的卡片，轨迹在评分后才用于更新。

### 4. 资源与算力

论文**未明确说明**所使用的GPU型号、数量、训练时长等算力资源。仅提到使用Qwen3-4B模型，且所有方法均为推理阶段操作（无需训练）。因此无法给出具体算力信息，但可推测由于无需训练，算力需求主要来自推理和卡片构建/协调的LLM调用，通常可在单块GPU（如A100 40G）上完成实验。

### 5. 实验数量与充分性

- **实验数量**：  
  - 主实验结果：3个主机 × 4个基准 = 12组完整对比，报告了12种方法的性能。  
  - 消融实验：在AutoGen和CAMEL上对KodCode和TriviaQA进行了4种组件消融（无图扩展、无协调、无失败反思、无失败准入）。  
  - 附加分析：  
    - 记忆库动态（活跃卡片数、负卡片比例、边类型分布）  
    - 协调压缩率（剪枝率）  
    - 案例研究（子图分析）  

- **充分性与公平性**：  
  - 覆盖了多种任务类型（QA、代码、规划）和多个MAS框架（3个主机）。  
  - 所有方法在同一骨干模型、同一预算、同一任务顺序下比较，确保了公平性。  
  - 消融实验验证了每个组件的贡献，且实验数量足以支持结论。  
  - 缺点：仅使用一个LLM骨干（Qwen3-4B），可能限制了泛化性；PDDL任务数较少（60个），统计显著性可能不足。

### 6. 主要结论与发现

- ConMem在所有三个主机和四个基准上均取得一致的正向增益，平均提升10.9-12.9个绝对百分点（相对于无记忆基线）。  
- ConMem在15个主机-指标比较中8次第一，7次第二，平均优于LatentMem（0.95-1.81点）并与ReMe相当或略优。  
- 协调模块能剪枝超过50%的扩展候选（规划任务中超过80%），表明瓶颈不在检索数量而在候选一致性。  
- 消融实验表明：图扩展、协调、失败反思和失败准入均提供互补增益，去除任一组件都会导致性能下降。  
- 案例研究表明：负卡片可传递失败衍生的验证过程，而非简单答案。

### 7. 优点

- **无需训练**：完全基于提示控制，不修改模型权重或主机编排，实现轻量自适应。  
- **结构化与关系感知**：卡片编码state/plan/exec/eval，图支持四种边类型，能显式处理依赖与冲突。  
- **预算控制**：通过量化校准的记忆预算和运行时序列化，确保提示长度可控。  
- **跨架构泛化**：在AutoGen、CAMEL、MacNet三种不同MAS主机上均有效，无需逐主机调参。  
- **可解释性**：卡片和图的显式结构便于审计与调试。  
- **理论支撑**：提供了关于卡片充分性、检索遗憾、负卡片激活阈值、图闭包等形式的定理。

### 8. 不足与局限

- **控制器质量依赖LLM**：卡片提取、失败反思、关系判断仍依赖LLM调用，错误可能传播。  
- **固定阈值与启发式规则**：当前使用校准阈值和显式协调规则，未采用学习策略，可能非最优。  
- **主机能力上限**：记忆不能创造宿主模型根本缺乏的技能。  
- **实验范围有限**：  
  - 仅使用一个LLM骨干（Qwen3-4B），未展示在更大模型（如GPT-4）上的表现。  
  - PDDL任务仅60个，统计效力较低。  
  - 未测试真实世界多轮交互或开放域长流程场景。  
- **潜在风险**：持久记忆可能保留敏感信息、积累捷径、或保留不安全策略，虽可审计但操作上需治理。

（完）
