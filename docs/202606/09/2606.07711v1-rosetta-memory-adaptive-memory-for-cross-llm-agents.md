---
title: "Rosetta Memory: Adaptive Memory for Cross-LLM Agents"
title_zh: "Rosetta Memory: 面向跨LLM智能体的自适应记忆"
authors: "Hao Yang, Shiqi Shen, Haoxuan Li, Zhipeng Wang, Zhi Gong, Xu Chen"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07711v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 跨LLM代理的自适应记忆系统
tldr: 用户频繁切换LLM（如用Claude编码、GPT写作）导致记忆系统难以跨模型复用。现有方案以LLM为中心设计记忆操作，忽略了上游记忆需适应下游不同模型的问题。本文转以记忆为中心，从写和读两侧设计profile条件算子，通过最小增益采样课程和性能差距奖励联合训练。实验表明，该方法在多个多跳QA数据集上优于基线，且对未见模型保持鲁棒，首次系统解决了跨LLM记忆适应问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07711v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1426, \"height\": 872, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 348, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1349, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1433, \"height\": 1184, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1456, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1455, \"height\": 719, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07711v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1459, \"height\": 717, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07711v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 376, \"label\": \"Table\"}]"
motivation: 用户频繁切换不同LLM导致上游记忆无法有效激活下游模型，现有LLM中心记忆系统缺乏跨模型适应性。
method: 提出记忆中心视角，设计写和读两端profile条件算子，用最小增益采样课程和性能差距奖励联合训练算子。
result: 在HotpotQA、2WikiMultihopQA和MuSiQue上一致优于基线，未见模型替换时仍保持鲁棒性能。
conclusion: Rosetta Memory实现了跨LLM记忆适应，使记忆存储和呈现能自适应不同下游模型，提升任务完成效果。
---

## 摘要
记忆是将无状态LLM转变为持久、不断进化的智能体的关键组件，通过经验积累、长期规划和持续自我改进实现。现有的记忆系统通常以LLM为中心，并针对特定主干设计记忆操作。然而，在实践中，用户经常在不同LLM之间切换，例如在使用Claude进行编码和GPT进行写作的任务中，或在单个任务中将不同步骤路由到不同主干以实现成本效益权衡。因此，一个模型写入的记忆往往需要被另一个模型消费。如何使上游记忆有效适应并激活下游LLM仍然是一个关键但未被充分探索的问题。为填补这一空白，我们将视角从以LLM为中心的记忆设计转向以记忆为中心的LLM适应。具体而言，我们从写入和读取两方面处理上述上下游记忆适应问题，并设计了两个基于配置文件的算子，通过联合训练优化记忆的存储和呈现方式，以更好地完成任务。为确保所学算子能泛化到广泛的LLM集合，我们提出了一种最小增益采样课程，在训练中优先考虑服务最少的LLM。为了更好衡量算子的实际贡献而非LLM自身能力，我们设计了一个性能差距奖励，与朴素记忆基线进行比较。在HotpotQA、2WikiMultihopQA和MuSiQue上的实验表明，我们的模型持续优于基线，并且在未见模型替换下保持稳健。

## Abstract
Memory is the key component for transforming a stateless LLM into a persistent, evolving agent through experience accumulation, long-horizon planning, and continual self-improvement. Existing memory systems typically take the LLM as the center and design memory operations tailored to a specific backbone. In practice, however, users frequently switch between LLMs, for example using Claude for coding and GPT for writing across tasks, or routing different steps to different backbones within a single task for cost-effective trade-offs. As a result, memory written by one model often needs to be consumed by another. Making upstream memory effectively adapt to and activate downstream LLMs remains a critical yet underexplored problem. To bridge this gap, we shift the perspective from LLM-centric memory design to \emph{memory-centric LLM adaptation}. Specifically, we approach the above upstream-downstream memory adaptation problem from both the write and read sides, and design two profile-conditioned operators that are jointly trained to optimize how memory is stored and presented for better task completion. To ensure the learned operators generalize across a broad set of LLMs, we propose a minimum-gain sampling curriculum that prioritizes the least-served LLMs during training. To better measure the operators' actual contribution rather than the LLM's own capability, we design a performance-gap reward that compares against a naive memory baseline. Experiments on HotpotQA, 2WikiMultihopQA, and MuSiQue demonstrate that our model consistently outperforms baselines and remains robust under unseen-model replacement.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入、客观的中文总结。

# 论文总结：Rosetta Memory: 面向跨LLM智能体的自适应记忆

## 1. 论文的核心问题与整体含义（研究动机与背景）

- **核心问题**：当前基于LLM的智能体系统，其记忆模块通常针对单一的、固定的LLM骨架进行设计（LLM-centric）。然而，在实际应用中，用户会频繁地跨任务或跨步骤切换不同的LLM（例如，用Claude编码，用GPT写作）。这导致了“记忆不匹配”问题：由上游LLM（写入方）产生的记忆，可能无法被下游LLM（读取方）有效理解和利用，从而影响任务性能。该问题在现有的记忆系统中尚未得到充分研究。
- **研究动机**：论文通过预实验（Figure 1）证明，当同一个上下文被不同LLM处理时（写入端），或当语义等价的记忆被不同LLM读取时（读取端），都会产生显著的回答差异，验证了跨LLM记忆适应问题的必要性和紧迫性。
- **整体含义**：该论文提出将视角从“以LLM为中心的记忆设计”转向“以记忆为中心的LLM适应”（*memory-centric LLM adaptation*），致力于解决记忆作为可转移接口在异构LLM系统中如何有效工作的问题。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过设计并联合训练两个**LLM感知（LLM-aware）的算子**——写入算子（*fwrite*）和读取算子（*fread*）——来适应性地调整记忆的存储和呈现方式，从而使其能够有效激活下游的任意目标LLM。
- **关键技术细节**：
    1.  **Profile条件化算子**：
        *   *fwrite*和*fread*被参数化为两个独立的语言模型（基于FLAN-T5-small）。
        *   每个LLM被压缩成一个轻量级的**Profile嵌入**（profile embedding），包含模型ID、文本描述和元数据。
        *   该Profile嵌入作为软前缀（soft prefix）注入到*fwrite*和*fread*中，指导算子生成针对特定目标模型风格的记忆。
    2.  **联合训练框架（Expert Iteration）**：
        *   利用带奖励过滤的Expert Iteration循环进行训练。首先，当前算子在混合LLM的轨迹上生成数据；然后，根据终端奖励保留最高奖励的前α部分轨迹作为专家示范；最后，通过监督学习优化算子模仿这些专家的读写决策。
    3.  **性能差距奖励（Performance-Gap Reward）**：
        *   为了区分算子的贡献与LLM自身的能力，设计了一个**反事实增益（Counterfactual Gain）**：$\Delta R(L^{(k)}) = R_f(L^{(k)}) - R_{\text{naive}}(L^{(k)})$。其中，$R_f$是使用所训练算子的期望奖励，$R_{\text{naive}}$是使用朴素基线（直接拼接所有记忆）的期望奖励。增益越低，说明该LLM在当前算子下越“未得到良好服务”。
    4.  **最小增益采样课程（Minimum-Gain Sampling Curriculum）**：
        *   在训练过程中，为了优先训练当前算子表现较差的LLM，按照以下概率采样每个步骤使用的LLM：
            $p(\ell_t = L^{(k)}) \propto d^{(t)}_k \exp\left(-\eta \Delta R(L^{(k)})\right)$
        *   其中，$d^{(t)}_k$是当前候选模型与已采样模型在嵌入空间的最小距离，用于鼓励模型多样性；$\exp(-\eta \Delta R)$项则倾向于选择增益较低的（即服务较差的）LLM。
    5.  **整体训练损失**：
        联合优化目标是通过对专家轨迹中的写入和读取步骤进行最大似然估计：
        $\mathcal{L}_{train} = -\mathbb{E}_{D_{train}} [\log p_{\theta_w}(m_t | x_{wt}) + \log p_{\theta_r}(\tilde{m}_t | x_{rt})]$

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集与场景**：使用三种多跳问答（Multi-hop QA）数据集作为代表性测试场景，以评估复杂的证据链积累与传递能力：
    *   **HotpotQA** (2跳)
    *   **2WikiMultihopQA** (3跳)
    *   **MuSiQue** (4跳)
- **Benchmark**：构建了由6个不同API模型组成的LLM池：gpt-5.4-mini, gpt-5.4-nano, gemini-2.5-flash, gemini-2.5-flash-nothinking, claude-sonnet-4-6, claude-sonnet-4-6-thinking。
- **对比方法**：将所有基线方法分为三类：
    *   **无记忆基线（Memory-free）**：Direct, Chain-of-Thought (CoT), Scratchpad, Self-Ask, Least-to-Most。
    *   **原始缓冲记忆基线（Raw-buffer memory）**：Step-Accumulate（拼接步骤信息）, Running-Summary（生成摘要）。
    *   **结构化记忆基线（Structured memory）**：Mem0, A-Mem, Memento。

## 4. 资源与算力

- **文中明确提到**：所有实验在**NVIDIA A800-SXM4-80GB GPU**上运行。
- **未明确指出**：具体使用了多少张GPU以及训练总时长。文中仅指出训练学习率、批大小等超参数，但未提供完整的大规模训练时间统计。

## 5. 实验数量与充分性

- **实验数量**：论文设计并进行了多组实验，涵盖：
    *   **主实验**（RQ1）：在3个数据集上与所有基线方法进行性能对比。
    *   **消融实验**（RQ2）：单独移除fwrite、fread和profile嵌入，分析各组件贡献。
    *   **泛化性实验**（RQ3）：测试在6个未见过的LLM（如gpt-5-mini, deepseek-reasoner等）替换部分训练模型后的性能。
    *   **敏感性分析**（RQ4）：测试最小增益采样系数 $\eta$ 对性能的影响。
    *   **定性分析**（RQ5）：展示读写操作的实际文本案例。
- **充分性与公平性**：
    *   **充分性**：实验设计较为全面，不仅关注总体性能，还深入分析了关键组件、泛化能力和超参数影响，并提供了定性案例，回答了5个明确的研究问题。附录中还包含了细致的写入-读取兼容性热力图和在噪声记忆下的鲁棒性分析。
    *   **公平性**：所有方法在相同的数据集子集、候选上下文来源、答案模型和评估指标下进行评估。主实验报告了4个随机种子的平均值（未提供方差分析），增强了结果的可靠性。

## 6. 论文的主要结论与发现

1.  **有效性**：提出的Rosetta Memory（RoMem）在所有三个多跳QA数据集上的所有指标（EM、F1、Acc、Acc_LLM）上均一致且显著地优于所有对比的基线方法。
2.  **组件贡献**：消融实验表明，Profile嵌入和写入算子（*fwrite*）对保持性能至关重要，读取算子（*fread*）在更具挑战性的跨模型迁移场景中增加鲁棒性。
3.  **泛化能力**：在将训练集中的部分LLM替换为未见过的模型时，RoMem仍能保持较好的性能，且在大多数情况下优于基线，证明了其对于新模型的良好泛化性。
4.  **采样策略效果**：最小增益采样系数 $\eta$ 对性能有显著影响，适中的重加权强度（如 $\eta=1.2$ 或 $1.6$）效果最佳，过强或过弱都会导致性能下降。
5.  **记忆形式的转变**：定性分析显示，训练后的写入算子能将不规范的源记忆转化为结构化、中立的证据；读取算子能根据目标LLM的特性，为其定制格式和提示风格，而非直接给出答案，从而实现更有效的激活。

## 7. 优点

1.  **问题新颖且明确**：论文首次正式提出并系统地定义了“跨LLM记忆适应”问题，切中实际应用中的关键痛点。
2.  **方法设计巧妙**：将记忆视为可转移的接口，通过Profile条件算子和反事实增益最小化采样，有效隔离了模型自身能力与记忆系统的贡献，设计思路合理且具有启发性。
3.  **实验论证扎实**：从主任务表现、消融、泛化、参数敏感性到定性和错误分析，实验覆盖全面，论证充分，有效支持了其核心观点。
4.  **视角转换**：从LLM中心转向记忆中心，为解决异构LLM系统中的协作问题提供了新的思路。

## 8. 不足与局限

1.  **任务场景有限**：论文仅在多跳QA场景下进行了评估，对于开放式的网页导航、工具使用、长程对话或规划等更复杂、更常见的智能体任务，其适用性有待验证。
2.  **数据规模限制**：实验使用了经过过滤的较小评估子集以适应API调用的成本，更大的模型池和更大的评估集将能提供更强有力的证据。
3.  **失效情况**：论文承认其方法仍存在失败情况，例如写入算子可能继承源LLM的不确定性、误将最终答案写入记忆；读取算子可能过度压缩信息，导致弱模型无法理解。
4.  **鲁棒性与风险**：论文在附录中明确指出了潜在风险。如果记忆源包含错误、偏见、隐私或受版权保护的内容，学习的写入算子可能保留这些有害信息，读取算子可能将其以更权威的形式传播，且跨模型转移会进一步放大风险。实用部署需要严格的数据控制、审计和人工审查机制。
5.  **资源开销**：虽然文中仅提及使用单个GPU型号，但训练和运行两个独立的LM（fwrite, fread）以及联合的profile编码器g，在更大模型和更长轨迹下会带来额外的计算和内存开销。

（完）
