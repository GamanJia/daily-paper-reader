---
title: "Selection Integrity for LLM Graph Memory: An Accumulability Criterion for Information-Flow-Blind Retrieval"
title_zh: LLM图内存的选择完整性：信息流盲检索的可累积性准则
authors: "Zeming Fei, Hongming Fei, Xiaoyang Wang, Yang yang, Prosanta Gope, Biplab Sikdar, Ying Zhang"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12290v1"
tags: ["query:agent-memory"]
score: 8.0
evidence: 提出图记忆检索的选择完整性准则
tldr: "LLM图记忆存在选择完整性漏洞，现有基于来源的防御无法阻止无源结构写入引发的隐蔽信道。通过累积性准则分析，发现Personalized PageRank易受攻击而Graphiti的节点距离免疫。AuthSelect方法通过重新计算认证子图的选择，成功阻止了所有攻击，仅增加2-3%延迟。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12290v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 291, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12290v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 807, \"height\": 369, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12290v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1717, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12290v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1440, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12290v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1799, \"height\": 381, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12290v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 209, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12290v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 875, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12290v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1826, \"height\": 1171, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12290v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 849, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12290v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 857, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12290v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 771, \"height\": 129, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12290v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 736, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.12290v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 896, \"height\": 680, \"label\": \"Table\"}]"
motivation: 现有图记忆防御仅检查记录来源，但结构写入可改变选择结果，导致信息泄露，需更根本的选择完整性保护。
method: 提出累积性准则，分析选择器对结构重分配的容限，区分易受攻击（如PageRank）与免疫（如节点距离）方法。
result: "AuthSelect在真实多会话记忆基准上零误拦，阻止28次转账攻击，延迟仅增2-3%。"
conclusion: 关闭隐蔽信道必须基于认证子图重新计算选择，而不是依赖来源标记。
---

## 摘要
智能体记忆正转向图结构，而目前为其构建的溯源防御均只检查一点：智能体检索到的记录之溯源。我们表明，这类防御在构造上就是盲目的。长期图记忆在可写图结构上运行全局选择步骤，因此不可信主体写入的结构会改变哪些认证事实被选中，而引用的证据保持完全认证；忠实的信息流控制（IFC）检查读者所用内容（全部已认证）的溯源，在文档QA基础和多会话智能体记忆上，其逐字节决策与无防御完全相同。在最关键的例子中，一条无源结构性写入在499个活跃操作中悄然误导了28次不可逆账本转移：忠实IFC允许每一次，而\authselect\阻止每一次。接着，我们精确刻画哪些记忆易受攻击：当选择器的结构项能将top-k成员中Ω(1)的份额重新分配到选定事实的边界之外时，该选择器便允许此信道。个性化PageRank可行，因为无源写入会重新路由守恒的随机游走质量；而基于内容的固定重新排序器则不可行，Graphiti的节点距离比PageRank更依赖结构，但仍保持免疫。可重新分配性（而非依赖性）是预测指标。我们一般性地证明了免疫情况，并在我们验证的瓶颈条件下证明了开放情况。关闭信道迫使任何溯源防御在认证子图上重新计算选择，这正是\authselect\所做的，且零超块、延迟仅增加2–3%。

## Abstract
Agent memory is moving to graphs, and the provenance defenses now being built for it all check one thing: the provenance of the records an agent retrieves. We show that this entire class of defense is blind by construction. A long-term graph memory runs a global selection step over writable graph structure, so structure that an untrusted principal writes changes \emph{which} authenticated facts are selected while the cited evidence stays fully authenticated; faithful information-flow control (IFC), checking the provenance of what the reader uses (all of it authenticated), makes the byte-identical decision to no defense at all, across document-QA substrates and real multi-session agent memory. In the most consequential instance, a no-source structural write silently misdirects $28$ irreversible ledger transfers over $499$ live actions: faithful IFC permits every one, and \authselect\ prevents every one. We then characterize exactly which memories are exposed: a selector admits the channel when its structural term can reallocate an $Ω(1)$ share of top-$k$ membership past a selected fact's margin. Personalized PageRank can, since a sourceless write reroutes conserved random-walk mass; a content-fixed reranker cannot, and Graphiti's node-distance, which leans on structure \emph{more} than PageRank does, stays immune. Reallocatability, not reliance, is the predictor. We prove the immune case in general and the open case under a chokepoint condition we verify. Closing the channel forces any provenance defense to recompute selection on the authenticated subgraph, which is what \authselect\ does, at zero over-block and $2$--$3\%$ latency.

---

## 论文详细总结（自动生成）

## 论文深度总结

### 1. 论文的核心问题与整体含义
- **研究动机**：LLM 的长期记忆正向图结构演化（如 HippoRAG2、Graphiti），但现有安全防御（如溯源认证、共识检查、信任评分）均只检查**检索到的记录**的来源，忽视了图记忆中的**全局选择步骤**（如 Personalized PageRank 或社区划分）会受不可信结构写入的影响。
- **核心问题**：不可信主体可写入无源边、合并链接等图结构，这些结构不携带任何可检索文本，但却能改变**哪些认证过的记录被选中**。此时读者引用的所有记录都是认证的，所以基于记录来源的信息流控制（IFC）对这种“选择信道”完全盲目，甚至与无防御的逐字节决策相同。
- **整体含义**：必须将图记忆的选择过程纳入安全边界，提出了选择完整性（Selection Integrity）的概念，并给出了一个可检验的**累积性准则**来区分哪些选择器易受攻击、哪些免疫。

### 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：选择信道是否打开取决于选择器的结构项能否**重新分配** top‑k 成员资格。若结构项能通过无源写入将守恒的随机游走质量导向错误节点，则信道打开（如 PageRank）；若候选集由内容固定、结构仅做有界重排，则免疫（如 Graphiti 的节点距离重排器）。
- **关键技术细节**：
  - 定义了**可累积性准则**：选择器 s(i) = g(c(i,Q), σ(i;G,Q)) 中，当 σ 满足 (a) 成员资格可重分配性（无源写入可转移 top‑k 中的 Ω(1) 份额）且 (b) 超过目标的锚定裕度时，D 非空。
  - 证明**P-vs-S 分歧定理**：忠实溯源防御（P）与选择完整性防御（S）在纯认证但选择被扰动的情况下完全分歧，且任何后选择防御（如过滤、重排检索结果）都无法关闭该信道。
  - 利用 Personalized PageRank 的质谱分裂：π(γ) = α·s_Q(γ)（遥传地板） + 游走质谱。地板不受边写入影响，因此低地板（多跳金标）易被驱逐；高地板（直接种子）不可驱逐。
  - 提出了 **AuthSelect 算法**：
    1. 在全图上执行原生选择 → a_G。
    2. 移除所有未认证的结构写入（按写入来源标签或插入顺序尾）得到认证子图 G_auth。
    3. 在 G_auth 上重放同一选择器 → a_Gauth。
    4. 若 a_G 与 a_Gauth 一致则接受 a_G，否则使用 a_Gauth 或按动作权限升级。
- **算法流程**（文字说明）：
  - 加载图之前记录初始边数 e0，每次未认证结构写入追加到 [e0,|E|) 尾部。
  - 防御时删除尾部边，重新运行与原生完全相同的选择器（如 PPR），比较诱导出的结果（答案或动作）。若不一致，则用认证子图的结果替代。

### 3. 实验设计
- **数据集**：
  - HotpotQA、MuSiQue、2WikiMultihopQA（多跳文档 QA，构建知识图谱）。
  - LoCoMo（真实多会话人类-AI 对话记忆），作为 realistic 场景。
- **场景**：
  - **文档 QA 答案危害**：在多跳问答中检测答案是否正确（通过精确子串匹配和 DeBERTa‑v3‑large NLI 判断）。
  - **实时外部动作**：调用 commit_action 向沙箱账本发起转账，模拟不可逆真实操作。
  - **多智能体共享记忆**：一个受损智能体（Writer）写入结构，受害者（Reader）读取并执行转账。
- **基准方法**：
  - **无防御**（直接使用全图选择）。
  - **忠实 IFC**：仅阻挡在 justification 中出现未认证记录，但对全认证选择变化不反应。
  - **上下文 IFC**：阻挡任何被检索的未认证记录。
  - **后选择防御**：贝叶斯信任（B‑SLM）、共识自检（B‑AMemGuard）、溯源重排（B‑Rerank）。
  - **原始已发表系统**：A‑MemGuard 原始审计器（使用更强的 72B 规划器）、生产级内存防火墙（Bayesian trust + pattern detection）。
- **攻击能力**：
  - 仅注入（Inject‑only）：添加恶意文本，通过 OpenIE 产生边。
  - 结构写入（Structural‑write）：直接写入无源边（合并、边、共谋），包括**金标预知攻击者**（oracle）和**金标不可知攻击者**（gold‑agnostic）。
- **选择器类型测试**：
  - HippoRAG2 的 PPR（可累积），Graphiti 的 RRF（无结构）、节点距离重排（有界重排）、Leiden 社区划分（可累积），Mem0 的向量选择（无结构）。

### 4. 资源与算力
- 文中未明确给出 GPU 型号、数量或训练时长（主要评估推理阶段）。
- 使用的模型：qwen2.5‑72b 作为规划器（temperature 0），qwen2.5‑7b 用于提取/识别，facebook/contriever 作为嵌入模型。
- 防御开销：AuthSelect 增加一次额外检索（PPR 传播 0.038–0.052 秒，占总延迟 2–3%），无需重新调用 LLM（除非选择发生分歧）。
- 总体算力需求来自多次推理（多数据集、多种子、多防御方法），但未提供具体核算。

### 5. 实验数量与充分性
- **实验数量**：
  - 在 4 个数据集上进行，最大规模 HotpotQA 使用 400 问（228 clean‑correct），MuSiQue 150 问，2Wiki 34 问，LoCoMo 10 个完整对话（约 585 行）。
  - 每个实验重复 2–3 个随机种子，使用 cluster bootstrap 计算置信区间。
  - 消融实验：边缘权重异常检测（阈值 >2 无效）、预算扫描（1→16 边）、累积写入、溯源完备性（召回率 0.6→1.0）、随机解码器（温度 0.7）。
- **充分性与客观性**：
  - 覆盖了多种攻击能力（注入 vs 结构写入）和多种选择器类型（PPR、RRF、节点距离、Leiden、向量）。
  - 对比了多个独立的后选择防御和原始已发表系统，保证公平。
  - 对主要结果（信道打开）进行了多数据集复制，并在双法官判断下报告置信区间。
  - 对免疫情况（如节点距离、RRF、Mem0）进行了具体测量，证实零盲行。
- **潜在偏差**：数据集问题对齐（非 web 规模）、可累积实例主要基于 PPR（Leiden 样本较小）、可用性权衡仅测了 n=9 的良性未认证结构。

### 6. 主要结论与发现
- **选择信道真实且危险**：在 HippoRAG2‑PPR 上，无源结构写入可导致 28 次不可逆账本转移被误导，忠实 IFC 允许全部，AuthSelect 阻止全部。
- **忠实溯源防御完全盲目**：在所有信道攻击行上，IFC 的决策与无防御逐字节相同，证明了 定理1（P-vs-S 分歧）和 推论1（不可约性）。
- **累积性准则预测准确**：
  - 结构依赖 ≠ 易受攻击：Graphiti 节点距离比 PPR 更依赖结构（Sdom=0.63 vs 0.24）但 0/28 盲行（免疫），因为其成员资格由内容固定，结构仅做有界重排。
  - 可重新分配性（Sadv）才是预测因子：PPR（Sadv 高）打开信道，RRF、节点距离、Mem0（Sadv ≈ 0）免疫。
- **AuthSelect 高效闭置信道**：
  - 在 4 个数据集上零误拦（clean‑correct 行一致时无操作），延迟增加仅 2–3%。
  - 在透明攻击者设定下（攻击者知道 AuthSelect），所有试图绕过的方式要么被移除（无源写入依旧被删），要么变为可见注入（被迫附带文本，落入 IFC 可见范围）。
- **注入边界**：仅注入能力（Inject‑only）无法在该系统上打开信道（0/35 行同时 stealthy & harmful），因为足够强的注入会变成可检索，从而被内容防御捕获。

### 7. 优点
- **理论贡献**：形式化定义了选择完整性，给出了 P-vs-S 分歧定理、不可约推论和累积性准则，将安全问题划分为可检验的准则。
- **防御新颖且实用**：AuthSelect 不是检测攻击，而是通过重算认证子图的选择来保证稳定性，设计上对称、语义清晰（写源标签自然决定结构是否认证）。
- **实验覆盖全面**：跨多个系统（HippoRAG2、Graphiti、Mem0）、多种选择器、多种攻击能力和真实多智能体场景，包括实时外部动作。
- **关键认知区分**：厘清了“结构依赖性”与“可重新分配性”的区别，并用反直觉的节点距离免疫例子验证了准则。
- **低开销**：零误拦（clean 行无差别），额外 LLM 调用只在选择分歧时触发，推理延迟仅增 2–3%。

### 8. 不足与局限
- **实验语料**：主要使用 QA 问答语料（HotpotQA、MuSiQue、2Wiki、LoCoMo），未扩展到 web 规模或开放域。
- **可累积实例**：主要结论基于 PPR（4 个数据集），Leiden 社区仅 38 个目标，样本量较小；其他累积选择器（如 GraphRAG 的全局摘要）未测试。
- **可用性权衡**：良性未认证结构（如智能体带来源的建议性记忆）的可用性损失仅在 n=9 的极小臂上测量，真实部署中可能影响用户体验。
- **危害范围**：测量了 QA 答案、一次工具调用和沙箱转账，未涉及不可逆真实世界操作（如发送邮件、控制物理系统）。作者声明了此范围。
- **溯源完备性假设**：AuthSelect 的有效性依赖于近乎完全的写入溯源。在部分召回（recall 0.6）下危害下降但未完全消除；实际部署中无源写入天然具有完整 U 标签，但复杂的派生结构可能需要更坚固的 taint 引擎。
- **注入边界**：仅注入无法打开信道是基于当前广告实验的测量结果，并非一般性定理，在其他系统或不同攻击策略下可能失效。

（完）
