---
title: "MemTrain: Self-Supervised Context Memory Training"
title_zh: MemTrain：自监督上下文记忆训练
authors: "Ziheng Li, Xingrun Xing, Haoqing Wang, Zhi-Hong Deng, Yehui Tang"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03197v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 自监督训练框架，增强LLM代理的上下文记忆能力
tldr: 现有记忆增强的大语言模型代理通常需要大量昂贵且多样性不足的下游任务标注数据进行端到端强化学习训练。本文提出MemTrain，一种自监督训练框架，利用无标注维基百科语料通过两个耦合代理任务增强上下文记忆能力：端到端掩码重建目标和中间记忆召回目标，并使用GRPO联合优化。在长文本问答和搜索问答基准上，MemTrain显著提升下游记忆密集型推理性能，最高提升17.67点。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03197v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1373, \"height\": 577, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03197v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1381, \"height\": 579, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03197v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1368, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03197v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1126, \"height\": 701, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03197v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 451, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03197v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03197v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1455, \"height\": 1193, \"label\": \"Table\"}]"
motivation: 现有记忆增强代理依赖昂贵且多样性不足的下游任务标注数据进行端到端训练，缺乏通用记忆能力。
method: 提出MemTrain框架，利用无标注维基百科设计掩码重建与中间记忆召回两个代理任务，通过GRPO联合优化提升通用记忆能力。
result: 在长文本QA和搜索QA基准上，相比直接任务专用训练，MemTrain提升最高17.67个点，且在不同模型上均有效。
conclusion: MemTrain作为一种自监督记忆训练方法，有效提升LLM代理在记忆密集型任务上的推理性能，且无需昂贵标注数据。
---

## 摘要
记忆是长周期LLM智能体不可或缺的能力，使其能够保留并利用跨扩展交互积累的信息。现有的记忆智能体方法通常在下游任务上使用强化学习进行端到端训练。然而，为记忆密集型场景收集高质量标注问题成本高昂，且生成的训练数据往往缺乏足够的多样性以覆盖通用的记忆行为。在这项工作中，我们提出MemTrain，一个自监督训练框架，用于普遍增强LLM智能体的上下文记忆能力，以实现更有效的下游后训练。MemTrain在未标记的维基百科语料上引入两个耦合的代理任务：（1）端到端掩码重建目标，要求模型在多轮记忆更新后恢复掩码实体，从而从最终结果角度鼓励记忆维护；（2）中间记忆回忆目标，要求模型使用中间记忆状态重建掩码的历史信息，鼓励在整个交互过程中进行忠实压缩和记忆完整性。这两个目标使用GRPO进行联合优化。在长文本问答和基于搜索的问答基准上的大量实验表明，MemTrain在不同模型上一致地提升了下游记忆密集型推理性能，相较于直接的任务特定后训练，最高取得了17.67个点的提升。

## Abstract
Memory is an indispensable capability for long-horizon LLM agents, enabling them to preserve and utilize information accumulated across extended interactions. Existing memory-agent approaches are typically trained end-to-end with reinforcement learning on downstream tasks. However, collecting high-quality annotated problems for memory-intensive scenarios is costly, and the resulting training data often lack sufficient diversity to cover general memory behaviors. In this work, we propose MemTrain, a self-supervised training framework for generally enhancing the context-memory capability of LLM agents for more effective downstream post-training. MemTrain introduces two coupled proxy tasks over unlabeled Wikipedia corpora: (1) an end-to-end masked reconstruction objective, which requires the model to recover masked entities after multiple rounds of memory updates, thereby encouraging memory maintenance from the final outcome perspective; and (2) an intermediate memory recall objective, which requires the model to reconstruct masked historical information using intermediate memory states, encouraging faithful compression and memory completeness throughout the interaction process. The two objectives are jointly optimized using GRPO. Extensive experiments on long-text QA and search-based QA benchmarks demonstrate that MemTrain consistently improves downstream memory-intensive reasoning performance across different models, achieving gains of up to 17.67 points over direct task-specific post-training.

---

## 论文详细总结（自动生成）

# 论文详细总结：MemTrain: Self-Supervised Context Memory Training

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长周期LLM智能体需要记忆能力来保存和利用跨多轮交互的信息。现有记忆增强智能体通常采用端到端强化学习（RLVR）进行下游任务训练，但存在两个关键瓶颈：
  - 收集高质量标注问题成本高昂（尤其记忆密集型场景）
  - 标注数据缺乏多样性，难以覆盖通用记忆行为，导致学到的记忆能力领域特定、泛化能力有限。
- **整体含义**：本文提出**MemTrain**——首个面向上下文记忆能力的自监督训练框架，旨在利用互联网上丰富的无标注文本（如维基百科）提升LLM智能体的通用记忆能力，为下游后训练提供更好的初始化，从而降低对昂贵标注数据的依赖并提升性能天花板。

## 2. 论文提出的方法论

### 核心思想
设计两个耦合的代理任务，分别从**结果级**和**过程级**提供监督信号，联合优化模型在记忆生成与利用上的表现。

### 关键技术细节
- **架构基础**：基于多轮上下文记忆框架（MemAgent），模型在每个交互步骤维护固定长度的记忆状态 `m_t`，输入为 `(m_{t-1}, a_{t-1}, i_t)`，输出更新后的记忆和动作。
- **代理任务一：端到端掩码重建（End-to-End Masked Reconstruction）**
  - 从维基百科随机选取一个段落作为“枢轴段落”，检索 n1 个语义相关段落 + N−n1−1 个随机段落，拼接成长文档。
  - 从枢轴段落中随机选取一个实体 y，在文档中所有出现位置替换为 [MASK]。
  - 文档被切分为固定长度块 `{c1,...,cT}`，模型依次读取并更新记忆，最终基于记忆输出预测掩码实体。
  - 监督信号：最终预测正确性 → 鼓励模型跨多轮记忆更新整合信息。
- **代理任务二：中间记忆召回（Intermediate Memory Recall, IMR）**
  - 在端到端轨迹完成后，随机选取中间步骤 k 和之前的一个块 `c_l (l<k)`，将该块中的实体替换为 [TARGET]。
  - 模型在单步内基于当前记忆 `o_{i,k}^E` 和掩码块预测目标实体。
  - 监督信号：中间记忆状态能否准确回忆历史信息 → 鼓励记忆保持完整性和忠实压缩。
- **联合优化**：使用 **GRPO**（Group Relative Policy Optimization）；每个训练样本采样 G1 个端到端轨迹，每个轨迹再采样 G2 个 IMR 轨迹。
  - 奖励设计：
    - IMR 奖励：`R^I_{i,j} = I[ˆy^I_{i,j} = ˜y_i]`
    - 端到端奖励：`R^E_i = I[ˆy^E_i = y] + (λ/G2) Σ_j R^I_{i,j}`，其中 λ = 0.5 平衡系数。
  - 优势估计：采用未归一化优势 `ˆA_{i,j,k} = R_i - mean{R_i}`，将轨迹奖励广播到所有步骤。
  - 损失函数：GRPO 目标（公式4），包含重要性采样裁剪和 KL 散度正则项。

### 算法流程（文字说明）
1. **数据构建**：从维基百科构建 N 个文档（掩码实体），文档被切分成长度一致的多块。
2. **采样轨迹**：当前策略采样 G1 个端到端多轮轨迹；对每个轨迹随机选择中间步骤，采样 G2 个 IMR 单步轨迹。
3. **计算奖励**：分别计算端到端精确匹配奖励和 IMR 精确匹配奖励，合并为总奖励。
4. **计算优势**：逐轨迹计算未归一化优势（减去组内均值）。
5. **策略更新**：对所有响应步骤（含端到端和 IMR）计算 GRPO 损失，更新策略参数。

## 3. 实验设计

### 使用数据集与场景
- **记忆训练数据**：维基百科无标注文本；构建30k个训练文档，每文档长度24k-40k tokens（使用spaCy NER识别实体，检索相似段落+随机段落）。
- **下游任务一：长文本多跳QA**
  - 基准：长文本HotpotQA（由Yu et al. 2025b提出），上下文长度从7k到896k tokens。
  - 场景：模型读取分块的长文档，回答多跳问题。
- **下游任务二：基于搜索的多跳QA**
  - 基准：7个标准数据集：TriviaQA, Bamboogle, HotpotQA, Natural Questions, PopQA, 2WikiMultiHopQA, MusiQUE。
  - 场景：模型使用搜索工具迭代检索外部信息，整合推理作答。

### 对比方法
- 基座模型：Qwen3-4B-Instruct-2507, Qwen2.5-7B-Instruct
- 下游后训练方法：
  - 任务一：**MemAgent** (Yu et al. 2025b)
  - 任务二：**MEM1** (Zhou et al. 2025a)
- 对比设置：基座模型直接推理、+MemTrain（仅自监督记忆训练）、+下游方法（直接后训练）、+MemTrain+下游方法。

### 实施细节
- 训练框架：基于veRL，GRPO，KL系数1e-3，DAPO式过滤（去除全0/全1奖励）。
- 上下文限制：8192 tokens（指令1024 + 输入块5120 + 记忆1024 + 响应1024），每个输入最多8个块。
- 训练步数：记忆训练300步；下游后训练：MemAgent 500步，MEM1 200步。
- 超参数：batch size=32，G1=8，G2=8，学习率1e-6（记忆训练）/ 5e-7（MEM1）。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量及总训练时长**。仅提及训练框架基于veRL，记忆训练300步，下游后训练500/200步，batch size 32等。未提供算力细节（如A100/H100数量、总GPU小时等）。

## 5. 实验数量与充分性

- **实验组数**：相当充分。
  - 两个主要任务（长文本QA + 搜索QA），覆盖多种上下文长度和7个不同数据集。
  - 两个不同规模基座模型（4B和7B）。
  - 消融实验（图3）：移除IMR（End-to-End变体）、端到端奖励与IMR解耦（Decoupled变体），对比完整模型。
  - 后训练扩展性比较（图4）：持续后训练300步 vs 记忆训练初始化。
  - 案例分析（表3）：定性展示记忆差异。
- **充分性与公平性**：
  - 对比条件一致：使用相同下游方法（MemAgent, MEM1），相同训练步数。
  - 报告平均性能及多长度/多数据集结果，消融实验检验各组件贡献。
  - 未明确说明是否使用多个随机种子，但提及“three different seeds”，合理。

## 6. 论文的主要结论与发现

- MemTrain 显著提升下游记忆密集型推理性能，且在不同模型、任务、上下文长度上**一致有效**。
  - 长文本QA：在Qwen2.5-7B上平均提升17.67点（55.86%→73.53%）；在Qwen3-4B上提升5.17点（65.14%→70.31%）。
  - 搜索QA：在Qwen3-4B上平均提升10.58点（21.50%→32.08%）；在Qwen2.5-7B上提升8.50点（23.94%→32.44%）。
- **长度泛化性强**：训练长度接近28k，但性能提升泛化到7k-896k，且在超长上下文中衰减更小。
- **自监督训练本身即可赋予一定记忆能力**：仅MemTrain（无下游后训练）在长文本QA上从约20%提升到约50%。
- **IMR组件关键**：移除IMR导致平均性能下降约7点（从70.31%降至63.28%）。
- **记忆训练优于扩展后训练步数**：即使继续后训练300步，MemTrain初始化仍保持优势（约2.6点）。

## 7. 优点

1. **首创性**：首次提出针对上下文记忆能力的自监督训练框架，区别于以往依赖标注数据的后训练方法。
2. **数据高效**：利用无标注维基百科，无需人工标注，可扩展到任意互联网文本，降低成本并提升多样性。
3. **双目标设计**：端到端 + 中间记忆召回，从结果和过程两个层面提供监督，弥补单一目标信号不足的问题。
4. **强泛化性**：在长度泛化、任务泛化（从长文本阅读到搜索QA）上均表现优异。
5. **兼容性**：可与任意下游记忆增强后训练方法结合，提升性能天花板。
6. **实验扎实**：覆盖两个代表性任务、多个数据集、消融、扩展性分析及案例，结论可靠。

## 8. 不足与局限

1. **数据来源单一**：仅基于维基百科，未验证在其他语料（如网页、对话、代码）上的泛化性。实际代理应用可能涉及多样化的领域和格式。
2. **算力信息缺失**：未提供训练所需GPU型号、数量、总时长，不利于复现和成本评估。
3. **模型规模限制**：仅测试4B和7B模型，未验证更大模型（如13B、70B）或更小模型上的效果。
4. **下游方法覆盖有限**：仅评估了MemAgent和MEM1两种后训练方法，更多记忆增强方法（如Memory-R1、MemSearcher等）未纳入。
5. **任务类型局限**：仅评估QA任务，未在规划、对话、工具使用等更复杂代理场景验证实际记忆行为。
6. **潜在偏差**：掩码实体的选择依赖于NER系统（spaCy），可能对特定类型实体有偏好。
7. **IMR设计假设**：中间记忆召回任务假设模型需要回忆历史块内容，但实际代理场景中记忆可能更抽象（如总结、推理链），简单召回不一定完全对应。

（完）
